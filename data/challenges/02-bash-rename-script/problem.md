# Bulk-rename Files in Bash

I want to rename all `.txt` files in a folder so that each one gets a `backup_` prefix.
The AI keeps suggesting convoluted regex pipelines through `sed`.

## Expected result
A simple, readable bash script — ideally a one-liner or a plain `for` loop — that does the job reliably and doesn't break on filenames that contain spaces.
